# Harbor Helm Values — Generic Configuration
# Container registry — internal-only deployment (HTTP, no TLS)
# See: https://github.com/goharbor/harbor-helm
#
# Environment-specific overrides (externalURL, existingSecretAdminPassword)
# are in the prod repo: values/harbor/values.yaml

# Use Recreate strategy for PVC-backed deployments (jobservice, registry).
# RollingUpdate causes Multi-Attach errors with RWO Longhorn volumes because
# the new pod tries to mount the volume while the old pod still holds it.
updateStrategy:
  type: Recreate

# Expose via LoadBalancer (Cilium LB-IPAM assigns IP)
expose:
  type: loadBalancer
  tls:
    enabled: false
  loadBalancer:
    name: harbor
    annotations: {}

# Admin password key name (secret reference is env-specific)
existingSecretAdminPasswordKey: admin-password

# Persistence using Longhorn (single replica)
persistence:
  enabled: true
  persistentVolumeClaim:
    registry:
      storageClass: longhorn
      size: 50Gi
    database:
      storageClass: longhorn
      size: 5Gi
    redis:
      storageClass: longhorn
      size: 1Gi
    trivy:
      storageClass: longhorn
      size: 5Gi

# Database — internal PostgreSQL
database:
  type: internal
  internal:
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi

# Redis — internal Redis
redis:
  type: internal
  internal:
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 250m
        memory: 256Mi

# Trivy — vulnerability scanning
trivy:
  enabled: true
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

# Core service
core:
  revisionHistoryLimit: 3
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

# Portal (web UI)
portal:
  revisionHistoryLimit: 3
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi

# Job service
jobservice:
  revisionHistoryLimit: 3
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

# Registry
registry:
  revisionHistoryLimit: 3
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi
