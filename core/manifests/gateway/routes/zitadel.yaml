apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: zitadel
  namespace: zitadel
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: cilium-system
  hostnames:
    - "zitadel.aaron.reynoza.org"
  rules:
    - backendRefs:
        - name: zitadel
          port: 8080
