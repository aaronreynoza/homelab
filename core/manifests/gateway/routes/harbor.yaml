apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: harbor
  namespace: harbor
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: cilium-system
  hostnames:
    - "harbor.aaron.reynoza.org"
  rules:
    - backendRefs:
        - name: harbor
          port: 80
