apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: open-webui
  namespace: ai
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: cilium-system
  hostnames:
    - "chat.aaron.reynoza.org"
  rules:
    - backendRefs:
        - name: open-webui
          port: 80
