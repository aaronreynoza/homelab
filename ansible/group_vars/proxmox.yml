---
# Proxmox WireGuard Client Configuration
wireguard_interface: wg0
wireguard_port: 51820
wireguard_client_ip: 10.10.10.2/32
wireguard_server_ip: "{{ hostvars[groups['wireguard_server'][0]]['ansible_host'] }}"
wireguard_allowed_ips: "10.10.10.0/24, 0.0.0.0/0"  # Adjust as needed
wireguard_server_public_key: ""  # This will be set during provisioning

# System settings
timezone: UTC
ssh_port: 22
